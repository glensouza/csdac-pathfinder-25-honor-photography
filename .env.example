# Google OAuth Configuration
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# PostgreSQL Configuration
# If using local PostgreSQL (docker-compose --profile postgres), just set password:
POSTGRES_PASSWORD=your_secure_postgres_password_here

# If using external PostgreSQL (e.g., separate LXC in Proxmox), set full connection string:
# Uncomment and configure the line below, and DO NOT use --profile postgres when deploying
# DB_CONNECTION_STRING=Host=your-postgres-host;Port=5432;Database=pathfinder_photography;Username=postgres;Password=your_password

# SigNoz Configuration (Optional)
# If using local SigNoz (docker-compose --profile signoz), leave this commented out
# If using external SigNoz (e.g., separate LXC in Proxmox), uncomment and set:
# OTEL_EXPORTER_OTLP_ENDPOINT=http://your-signoz-host:4317
# If not using SigNoz at all, set to empty:
# OTEL_EXPORTER_OTLP_ENDPOINT=

# Email Configuration for Gmail (Optional - leave empty to disable email notifications)
# To use Gmail:
# 1. Enable 2-Step Verification in your Google Account
# 2. Generate an App Password at https://myaccount.google.com/apppasswords
# 3. Use your Gmail address as the username
# 4. Use the 16-character App Password (not your regular password)
EMAIL_SMTP_HOST=smtp.gmail.com
EMAIL_SMTP_PORT=587
EMAIL_SMTP_USERNAME=your-email@gmail.com
EMAIL_SMTP_PASSWORD=your-16-character-app-password
EMAIL_USE_SSL=true
EMAIL_FROM_ADDRESS=your-email@gmail.com
EMAIL_FROM_NAME=Pathfinder_Photography
