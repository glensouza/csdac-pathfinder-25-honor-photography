@page "/top-photos"
@using PathfinderPhotography.Services
@using PathfinderPhotography.Models
@inject VotingService VotingService
@inject CompositionRuleService RuleService
@rendermode InteractiveServer

<PageTitle>Top Ranked Photos</PageTitle>

<div class="container mt-4">
    <h1>🏆 Top Ranked Photos</h1>
    <p class="lead">Photos ranked by community votes using the ELO rating system</p>

    <div class="mb-3">
        <p class="text-muted">
            <small>
                <strong>How it works:</strong> Each photo starts with a rating of 1000. 
                When people vote, ratings are adjusted based on expected outcomes. 
                Higher ratings indicate photos that consistently win comparisons.
            </small>
        </p>
    </div>

    <div class="mb-4 d-flex gap-2 align-items-center">
        <label class="form-label mb-0">Filter:</label>
        <select class="form-select w-auto" @onchange="OnGradeFilterChanged">
            <option value="All">All</option>
            <option value="Pass">Passed</option>
            <option value="NotGraded">Not Graded</option>
            <option value="Fail">Failed</option>
        </select>
        <small class="text-muted ms-3">Showing top 3 per rule</small>
    </div>

    @if (isLoading)
    {
        <div class="text-center my-5">
            <div class="spinner-border" role="status">
                <span class="visually-hidden">Loading...</span>
            </div>
            <p class="mt-2">Loading top photos...</p>
        </div>
    }
    else
    {
        @foreach (CompositionRule rule in rules)
        {
            <div class="mb-5">
                <h2 class="mb-3">@rule.Id. @rule.Name</h2>
                <div class="row">
                    @if (topPhotosByRule.TryGetValue(rule.Id, out var photos) && photos.Any())
                    {
                        @foreach ((PhotoSubmission photo, int index) in photos.Select((p, i) => (p, i)))
                        {
                            <div class="col-md-6 col-lg-4 mb-4">
                                <div class="card h-100 @GetRankClass(index)">
                                    @if (index < 3)
                                    {
                                        <div class="position-absolute top-0 start-0 m-2">
                                            <span class="badge @GetBadgeClass(index) fs-5">@GetRankIcon(index) #@(index + 1)</span>
                                        </div>
                                    }
                                    <img src="/api/images/@photo.Id" 
                                         class="card-img-top" 
                                         alt="@photo.CompositionRuleName by @photo.PathfinderName"
                                         style="height: 300px; object-fit: cover; cursor: pointer;"
                                         @onclick="() => ShowModal(photo)" />
                                    <div class="card-body">
                                        <div class="d-flex justify-content-between align-items-start mb-2">
                                            <h6 class="card-title mb-0">
                                                @if (index >= 3)
                                                {
                                                    <span class="badge bg-secondary me-1">#@(index + 1)</span>
                                                }
                                                @photo.CompositionRuleName
                                            </h6>
                                            <span class="badge bg-info">@Math.Round(photo.EloRating, 0)</span>
                                        </div>
                                        <p class="card-text">
                                            <strong>By:</strong> @photo.PathfinderName<br />
                                            <small class="text-muted">@photo.SubmissionDate.ToString("MMM dd, yyyy")</small>
                                        </p>
                                        @if (photo.GradeStatus == GradeStatus.Pass)
                                        {
                                            <span class="badge bg-success">✓ Pass</span>
                                        }
                                        else if (photo.GradeStatus == GradeStatus.NotGraded)
                                        {
                                            <span class="badge bg-secondary">Not Graded</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-danger">✗ Fail</span>
                                        }
                                    </div>
                                </div>
                            </div>
                        }
                    }
                    else
                    {
                        <div class="col-12">
                            <div class="alert alert-info">No submissions for this rule yet.</div>
                        </div>
                    }
                </div>
            </div>
        }

        @if (!topPhotosByRule.Values.Any(list => list.Any()))
        {
            <div class="alert alert-info">
                <p class="mb-0">No photos have been ranked yet. <a href="/vote">Start voting</a> to build the rankings!</p>
            </div>
        }
    }

    <div class="mt-4">
        <a href="/vote" class="btn btn-primary">Vote on Photos</a>
        <a href="/" class="btn btn-outline-secondary ms-2">← Back to Home</a>
    </div>
</div>

@if (selectedSubmission != null)
{
    <div class="modal fade show d-block" tabindex="-1" style="background-color: rgba(0,0,0,0.5);">
        <div class="modal-dialog modal-lg modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">@selectedSubmission.CompositionRuleName</h5>
                    <button type="button" class="btn-close" @onclick="CloseModal"></button>
                </div>
                <div class="modal-body">
                    <img src="/api/images/@selectedSubmission.Id" 
                         class="img-fluid mb-3" 
                         alt="@selectedSubmission.CompositionRuleName" />
                    <p><strong>Pathfinder:</strong> @selectedSubmission.PathfinderName</p>
                    <p><strong>ELO Rating:</strong> @Math.Round(selectedSubmission.EloRating, 1)</p>
                    <p><strong>Submitted:</strong> @selectedSubmission.SubmissionDate.ToString("MMMM dd, yyyy")</p>
                    <p><strong>Description:</strong></p>
                    <p>@selectedSubmission.Description</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" @onclick="CloseModal">Close</button>
                </div>
            </div>
        </div>
    </div>
}

<style>
    .rank-gold {
        border: 3px solid gold;
        box-shadow: 0 0 15px rgba(255, 215, 0, 0.5);
    }

    .rank-silver {
        border: 3px solid silver;
        box-shadow: 0 0 15px rgba(192, 192, 192, 0.5);
    }

    .rank-bronze {
        border: 3px solid #cd7f32;
        box-shadow: 0 0 15px rgba(205, 127, 50, 0.5);
    }
</style>

@code {
    private List<CompositionRule> rules = [];
    private Dictionary<int, List<PhotoSubmission>> topPhotosByRule = new();
    private PhotoSubmission? selectedSubmission;
    private bool isLoading = true;

    protected override async Task OnInitializedAsync()
    {
        rules = RuleService.GetAllRules();

        // Load top 3 photos per rule using a single optimized query
        topPhotosByRule = await VotingService.GetTopPhotosByRuleBulkAsync(null, 3);

        // Ensure keys exist for rules with no submissions
        foreach (CompositionRule rule in rules.Where(rule => !topPhotosByRule.ContainsKey(rule.Id)))
        {
            topPhotosByRule[rule.Id] = [];
        }

        isLoading = false;
    }

    private async Task OnGradeFilterChanged(ChangeEventArgs e)
    {
        string value = e.Value?.ToString() ?? "All";
        GradeStatus? status = value switch
        {
            "All" => null,
            "Pass" => GradeStatus.Pass,
            "NotGraded" => GradeStatus.NotGraded,
            "Fail" => GradeStatus.Fail,
            _ => null
        };

        isLoading = true;
        topPhotosByRule = await VotingService.GetTopPhotosByRuleBulkAsync(status, 3);

        // Ensure keys exist for rules with no submissions
        foreach (CompositionRule rule in rules.Where(rule => !topPhotosByRule.ContainsKey(rule.Id)))
        {
            topPhotosByRule[rule.Id] = [];
        }

        isLoading = false;
        StateHasChanged();
    }

    private static string GetRankClass(int index)
    {
        return index switch
        {
            0 => "rank-gold",
            1 => "rank-silver",
            2 => "rank-bronze",
            _ => ""
        };
    }

    private string GetBadgeClass(int index)
    {
        return index switch
        {
            0 => "bg-warning text-dark",
            1 => "bg-light text-dark",
            2 => "bg-warning",
            _ => "bg-secondary"
        };
    }

    private static string GetRankIcon(int index)
    {
        return index switch
        {
            0 => "🥇",
            1 => "🥈",
            2 => "🥉",
            _ => ""
        };
    }

    private void ShowModal(PhotoSubmission submission)
    {
        selectedSubmission = submission;
    }

    private void CloseModal()
    {
        selectedSubmission = null;
    }
}
